[package]
name = "unsafe_password"
version = "0.1.0"
description = "."
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "unsafe_password"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.26.0"
